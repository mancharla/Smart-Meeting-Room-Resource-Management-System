[project]
name = "my-app"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    # add your other packages here (sqlalchemy, pydantic-settings, etc.)
]

[tool.vercel]
entrypoint = "backend.app.main:app"
